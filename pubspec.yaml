name: cpm
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.1
  flutter_secure_storage: ^9.2.2 
  intl: ^0.20.2
  
  firebase_core: ^3.3.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.1.0

  url_launcher: ^6.3.1

  crypto: ^3.0.3
  convert: ^3.1.1

  file_picker: ^8.0.0+1
  csv: ^6.0.0

  solana: ^0.31.2+1
  web3dart: ^2.7.1
  flutter_dotenv: ^5.1.0 # <-- DEPENDENCIA AÑADIDA
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logos/
    - .env # <-- ASSET AÑADIDO