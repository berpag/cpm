name: cpm
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.1 # <--- ¡AÑADE ESTA LÍNEA AQUÍ!
  flutter_secure_storage: ^9.2.2 
  intl: ^0.20.2 # <-- Puede que esta versión necesite actualizarse, pero probemos primero.
  
  firebase_core: ^3.3.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.1.0
  solana: ^0.31.2+1
  

  url_launcher: ^6.3.1

  crypto: ^3.0.3
  convert: ^3.1.1

  file_picker: ^8.0.0+1
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logos/